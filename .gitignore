# Rust build artifacts
target/
**/*.rs.bk

# Tauri generated files — platform-specific, never committed
src-tauri/gen/**/local.properties
src-tauri/gen/**/build/

# JavaScript test harness
node_modules/
coverage/
.vitest-cache/

# Agent tooling — local skill registry cache
.atl/

# Claude Code runtime — local locks and scheduled task state
.claude/

# Smoke test ephemeral outputs — transcripts persist in engram
*smoke*.log
*-trace.log
watchdog*.log
smoke-trace.ps1
