#![cfg(all(target_os = "windows", feature = "hw-encoder"))]
// TODO: WindowsMftH264Encoder — Media Foundation Transform hardware H.264 encoder.
// Implementation added in Phase 3 (struct + validation) and Phase 4 (encoder thread + pump).
